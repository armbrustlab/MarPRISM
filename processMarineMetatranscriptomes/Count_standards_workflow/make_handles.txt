# make handles 

ls *.6tr.bf40.orfs40.fasta.gz > G1NS.6tr.bf40.orfs40.handles.txt

#remove file extensions #sed -i -e 's/goodbye//g' filename

sed -i -e 's/.extendedFrags.6tr.bf40.fasta.gz//g' /mnt/nfs/ryan/Gradients2/NS/merged/6tr/bf40/G2NS.extendedFrags.6tr.bf40.handles.txt

###########
if text that needs to be removed has '/"
then use this code instead:
#remove directory extensions. Because the extensions have '/' characters the original 'sed -e s/deletethis//g -i *' code is buggy
#remove the first 
sed 's/^.\{,58\}//' G3_PA_underway_merged_handles.txt > G3_PA_underway_merged_handles_short.txt
